# book — book.yioh.ai.kr

로컬 작업 폴더: `/home/dspub/book`

연세대학교 산업보건연구소 교재의 **공개 빌드 결과물**만 담는 레포. GitHub Pages 로 `book.yioh.ai.kr` 에 서빙된다.

- 원고·문제은행·정답·docx 는 넣지 않는다 (공개 레포).
- 교재를 다시 빌드한 뒤 `./sync.sh && git add -A && git commit -m "update" && git push` 하면 몇 분 안에 반영된다.
- 연구소 대문(yioh.ai.kr) 자료실에는 이 주소를 외부 링크로 등록한다.
